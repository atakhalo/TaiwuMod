# 目录

- [目录](#目录)
- [1. 查询场景 Canvas 列表](#1-查询场景-canvas-列表)
- [2. 查询 UI 面板状态和子物体](#2-查询-ui-面板状态和子物体)
- [3. 查找 UI 面板中的按钮](#3-查找-ui-面板中的按钮)
- [4. 查询 Spine 动态立绘信息](#4-查询-spine-动态立绘信息)
- [5. 查询太吾姓名](#5-查询太吾姓名)
		- [途径 1：`taiwu_info`（推荐，最快捷）](#途径-1taiwu_info推荐最快捷)
		- [途径 2：`character_name`](#途径-2character_name)
		- [途径 3：`back_code` + `GetNameRelatedData`](#途径-3back_code--getnamerelateddata)
		- [途径 4：`front_code` + `BasicGameData`](#途径-4front_code--basicgamedata)
- [6. 查询世界信息](#6-查询世界信息)
- [7. 查询同道列表](#7-查询同道列表)
		- [途径 1：后端 `back_code`](#途径-1后端-back_code)
		- [途径 2：前端 `front_code`](#途径-2前端-front_code)
- [8. 查询角色魅力值（后端）](#8-查询角色魅力值后端)
- [9. 查询角色魅力值（前端）](#9-查询角色魅力值前端)
- [10. 查询前端 BasicGameData 全量](#10-查询前端-basicgamedata-全量)
- [11. 查询角色详细信息](#11-查询角色详细信息)
- [12. 查询地块角色信息](#12-查询地块角色信息)
	- [12.1 获取地块上的角色 ID](#121-获取地块上的角色-id)
	- [12.2 获取门派地块信息（扩展）](#122-获取门派地块信息扩展)
- [13. 查询当前 UI 层级结构](#13-查询当前-ui-层级结构)
	- [13.1 一键查询（推荐）](#131-一键查询推荐)
	- [13.2 手动查询步骤](#132-手动查询步骤)
		- [步骤 2：查询单个 Canvas 的排序值](#步骤-2查询单个-canvas-的排序值)
	- [13.3 游戏 UI 层级参考（实测数据）](#133-游戏-ui-层级参考实测数据)

---

# 1. 查询场景 Canvas 列表

**需求：** 查看当前游戏场景中有多少个 Canvas、路径分组

**请求（1次）：**

```json
{
    "requestId": "scene_canvases_001",
    "type": "scene_hierarchy",
    "params": {
        "rootType": "all_canvases"
    }
}
```

| 参数 | 值 | 说明 |
|:----|:---|:-----|
| `rootType` | `"all_canvases"` | 列出所有 Canvas |

**预期返回关键数据：**

```json
{
    "requestId": "scene_canvases_001",
    "success": true,
    "hasLongResult": true,
    "longResultFile": "result_scene_canvases_001.json"
}
```

从 `longResultFile` 中解析：

- 顶层数组每个元素是一个 Canvas 节点
- 每个节点含：`name`, `active`, `path`, `components[]`, `childCount`, `children[]`（递归深度 5 层）
- 通过分析 `path` 前缀可统计各摄像机下的 Canvas 数量

**关键路径提取命令 (PowerShell)：**

```powershell
# 统计 Canvas
$json | ForEach-Object { $_.name }
# 按路径前缀分组
$json | Group-Object { if ($_.path -match '^(.+?)/') { $matches[1] } else { 'RootLevel' } }
```

---

# 2. 查询 UI 面板状态和子物体

**需求：** 查询某个具体的 UI 面板（如 ViewBottom）是否显示，及其一级子物体列表

**请求（1次）：**

```json
{
    "requestId": "scene_viewbottom_002",
    "type": "scene_hierarchy",
    "params": {
        "rootType": "path",
        "path": "Camera_UIRoot/Canvas/LayerPart/ViewBottom"
    }
}
```

| 参数 | 值 | 说明 |
|:----|:---|:-----|
| `rootType` | `"path"` | 按路径查询 |
| `path` | 完整 GameObject 路径 | 从场景根开始 |

**预期返回关键数据（result 或 longResultFile）：**

```json
{
    "name": "ViewBottom",
    "active": true,
    "path": "Camera_UIRoot/Canvas/LayerPart/ViewBottom",
    "components": ["RectTransform", "Canvas", "ViewBottom", "ReadAndLoop", ...],
    "childCount": 3,
    "children": [
        { "name": "AnimationRoot", "active": true, "childCount": 26 },
        { "name": "CustomButtonPrefab", "active": false, "childCount": 1 },
        { "name": "Mask", "active": false, "childCount": 3 }
    ]
}
```

**关键字段：**

| 字段 | 说明 |
|:----|:-----|
| `active` | 是否显示（true/false） |
| `childCount` | 子物体数量 |
| `children[].name` | 子物体名称 |
| `children[].active` | 子物体是否显示 |
| `components[]` | 组件列表 |

---

# 3. 查找 UI 面板中的按钮

**需求：** 从已获取的层级数据中，找到所有按钮（Button/CButton/CustomButton）及其激活状态

**过程：** 使用前一请求返回的 `longResultFile` 数据，进行递归搜索

**提取命令 (PowerShell)：**

```powershell
function Find-Buttons($node, $depth=0) {
    $indent = '  ' * $depth
    if ($node.name -match '[Bb]utton' -or
        ($node.components | Where-Object { $_ -match '[Bb]utton' })) {
        Write-Host "${indent}[$compActual] $($node.name) active=$($node.active) path=$($node.path)"
    }
    if ($node.children) { foreach ($child in $node.children) { Find-Buttons $child ($depth+1) } }
}
Find-Buttons $json
```

**预期输出示例：**

```
[Button] MainCharacterButton active=True path=.../MainCharacterButton
[CButton] OpenCharMenu active=True path=.../OpenCharMenu
[CButton] Teammate1 active=True path=.../Teammate1
[CButton] WorldMap active=True path=.../MapButton/WorldMap
[CButton] MapFilter active=True path=.../MapButton/MapFilter
...
```

**匹配规则：** 节点名或组件名包含 `"Button"`（忽略大小写）

---

# 4. 查询 Spine 动态立绘信息

**需求：** 查询 NpcSpine 的骨骼名、皮肤、动画、缩放、纹理等信息

**步骤：** 多次 `component_info` / `component_field` 请求

| 步骤 | 请求类型 | 参数 | 预期结果 |
|:----|:--------|:-----|:---------|
| 4.1 查组件概况 | `component_info` | `gameObjectPath: ".../NpcSpine"`, `componentType: "SkeletonGraphic"` | 基础字段：`startingAnimation: "Loop"`, `timeScale: 1.0` |
| 4.2 查当前动画 | `component_field` | `componentType: "SkeletonGraphic"`, `field: "AnimationState"` | `_toString: "Loop"` |
| 4.3 查骨骼数据资产 | `component_field` | `componentType: "SkeletonGraphic"`, `field: "skeletonDataAsset"` | `_type: "SkeletonDataAsset"` |
| 4.4 查所属 Avatar | `component_info` | `gameObjectPath: ".../MainCharacterButton"`, `componentType: "Avatar"` | `PreferDynamicAvatar: true`, `Size: "Normal"` |
| 4.5 查骨骼资源名 | `component_field` | `componentType: "Avatar"`, `field: "_currentSpineName"` | `"Yj:vivian"` |
| 4.6 查皮肤 | `component_field` | `componentType: "Avatar"`, `field: "_currentSpineSkin"` | `""`（空=默认皮肤） |
| 4.7 查纹理 | `component_field` | `componentType: "SkeletonGraphic"`, `field: "mainTexture"` | `_toString: "vivian (Texture2D)"` |

**完整请求参数 (4.5)：**

```json
{
    "requestId": "field_spinename_009",
    "type": "component_field",
    "params": {
        "gameObjectPath": "Camera_UIRoot/Canvas/LayerPart/ViewBottom/AnimationRoot/MainCharacterButton",
        "componentType": "Avatar",
        "field": "_currentSpineName"
    }
}
```

**预期汇总：**

| 字段 | 示例值 | 说明 |
|:----|:------|:-----|
| `_currentSpineName` | `"Yj:vivian"` | Spine 骨骼资源名 |
| `_currentSpineSkin` | `""` | 默认皮肤（空字符串） |
| `mainTexture` | `"vivian"` | 主纹理名 |
| 当前动画 | `"Loop"` | 循环待机动画 |
| `timeScale` | `1.0` | 正常速度 |

---

# 5. 查询太吾姓名

**需求：** 获取太吾角色的姓名。有三种途径，推荐途径 1。

### 途径 1：`taiwu_info`（推荐，最快捷）

```json
{
    "requestId": "taiwu_001",
    "type": "taiwu_info"
}
```

**预期：**

```json
{
    "taiwuCharId": 6755,
    "taiwuCharacter": { "name": "竺梦堂", "age": 18, "gender": 1 }
}
```

### 途径 2：`character_name`

```json
{
    "requestId": "cname_001",
    "type": "character_name",
    "params": { "charId": 6755 }
}
```

**预期：**

```json
{ "surname": "竺", "givenName": "梦堂", "fullName": "竺梦堂" }
```

### 途径 3：`back_code` + `GetNameRelatedData`

通过 `back_code` 链式调用：

```json
{
    "requestId": "data_001",
    "type": "back_code",
    "params": {
        "entry": { "name": "GameData.Domains.DomainManager" },
        "chain": [
            { "step": "field", "name": "Character" },
            { "step": "method", "name": "GetNameRelatedData", "argTypes": ["System.Int32"], "args": [6755] },
            { "step": "field", "name": "FullName" }
        ]
    }
}
```

**预期：** `FullName` 结构体，含 `customSurnameId`、`customGivenNameId`

### 途径 4：`front_code` + `BasicGameData`

通过 `front_code` 从前端单例读取：

```json
{
    "requestId": "fe_001",
    "type": "front_code",
    "params": {
        "entry": { "name": "BasicGameData" },
        "chain": [
            { "step": "field", "name": "CustomTexts" }
        ]
    }
}
```

**预期：** `CustomTexts{0:"竺", 1:"梦堂", ...}`

---

# 6. 查询世界信息

**需求：** 获取游戏当前日期、年份、月份、世界ID

**请求（1次）：**

```json
{
    "requestId": "world_001",
    "type": "world_info"
}
```

**预期：**

```json
{
    "currDate": 36,
    "year": 3,
    "month": 1,
    "worldId": 600695272,
    "isFinishedInit": true
}
```

**说明：** `currDate` 是游戏内部月份计数（从 0 开始），`year = currDate / 12`，`month = currDate % 12 + 1`

---

# 7. 查询同道列表

**需求：** 获取太吾队伍中的所有同道角色 ID

### 途径 1：后端 `back_code`

```json
{
    "requestId": "team_001",
    "type": "back_code",
    "params": {
        "entry": { "name": "GameData.Domains.DomainManager" },
        "chain": [
            { "step": "field", "name": "Taiwu" },
            { "step": "field", "name": "_combatGroupCharIds" }
        ]
    }
}
```

**预期：** `[7059, 7068, 7066]`（int 数组）

### 途径 2：前端 `front_code`

```json
{
    "requestId": "fe_team_001",
    "type": "front_code",
    "params": {
        "entry": { "name": "CharacterMonitorModel" },
        "chain": [
            { "step": "method", "name": "GetTaiwuTeamCharIds" }
        ]
    }
}
```

**预期：** `List<int>`（序列化为 JSON 数组）

---

# 8. 查询角色魅力值（后端）

**需求：** 通过后端 API 获取角色的魅力值

**步骤：**

| 步骤 | 请求 | 说明 |
|:----|:-----|:-----|
| 8.1 查到同道 ID 列表 | 见 #7 | 获取 charIds |
| 8.2 查询姓名 | `character_name` | 确认角色 |
| 8.3 查魅力值 | `back_code` + 链式 | 详见下方 |

**请求（8.3）：**

```json
{
    "requestId": "charm_001",
    "type": "back_code",
    "params": {
        "entry": { "name": "GameData.Domains.DomainManager" },
        "chain": [
            { "step": "field", "name": "Character" },
            { "step": "method", "name": "GetElement_Objects", "argTypes": ["System.Int32"], "args": [7059] },
            { "step": "method", "name": "CalcAttraction" }
        ]
    }
}
```

**说明：** `Character` 域对象 → `GetElement_Objects(charId)` → `CalcAttraction()`

**预期：** `900`（int，魅力值）

---

# 9. 查询角色魅力值（前端）

**需求：** 通过前端 `CharacterMonitorModel` 获取角色的魅力值

**步骤：**

| 步骤 | 请求 | 说明 |
|:----|:-----|:-----|
| 9.1 查到同道 ID 列表 | 见 #7 途径 2 | 获取 charIds |
| 9.2 查魅力值 | `front_code` + `GetMonitorItem<T>` | 详见下方 |

**请求：**

```json
{
    "requestId": "fe_charm_001",
    "type": "front_code",
    "params": {
        "entry": { "name": "CharacterMonitorModel" },
        "chain": [
            {
                "step": "method",
                "name": "GetMonitorItem<CharacterDataMonitor.DetailInfoMonitor>",
                "argTypes": ["System.Int32", "System.Boolean"],
                "args": [7059, false]
            },
            { "step": "field", "name": "Attraction" }
        ]
    }
}
```

**说明：** 泛型方法 `GetMonitorItem<T>` 用 `MethodName<TypeName>` 格式调用，参数按顺序传入

**可用的 Monitor 类型：**

| Monitor 类型 | 关键属性 | 用途 |
|:-----------|:---------|:-----|
| `DetailInfoMonitor` | `Attraction`, `Fame`, `Happiness` | 魅力、名声、心情 |
| `BasicInfoMonitor` | `Gender`, `FavorabilityToTaiwu` | 性别、好感 |
| `AttributeMonitor` | `MaxMainAttribute`, `AtkHitValues` | 六维主属性、命中 |
| `AvatarInfoMonitor` | `AvatarData`, `AvatarAge` | 头像数据 |
| `AgeHealthMonitor` | — | 年龄健康 |
| `FeatureMonitor` | — | 特性 |

**预期：** `{ "_type": "...DetailInfoMonitor", "attraction": 900 }`

---

# 10. 查询前端 BasicGameData 全量

**需求：** 查看前端 BasicGameData 的所有公开字段

**请求（1次）：**

```json
{
    "requestId": "fe_full_001",
    "type": "front_code",
    "params": {
        "entry": { "name": "BasicGameData" }
    }
}
```

**预期：** 返回 `_type: "BasicGameData"` + 约 34 个公开字段，包括：

| 字段 | 说明 |
|:----|:-----|
| `TaiwuCharId` | 太吾角色 ID |
| `CurrDate` | 当前日期 |
| `WorldId` | 世界种子 |
| `CustomTexts` | 自定义文本表（含角色姓名） |
| `ActionPointCurrMonth` | 本月行动力 |
| `TaiwuVillageStateTemplateId` | 太吾村状态 |
| ... | 等 |

---

# 11. 查询角色详细信息

**需求：** 获取角色的完整字段数据

**请求（1次）：**

```json
{
    "requestId": "char_detail_001",
    "type": "character_info",
    "params": { "charId": 7059 }
}
```

**预期：** 返回 Character 对象的所有字段（深度 3 层），包括：

| 字段 | 说明 |
|:----|:-----|
| `templateId` | 角色模板 ID |
| `gender` | 性别（0=女, 1=男） |
| `actualAge` | 实际年龄 |
| `baseMainAttributes` | 基础六维属性 |
| `featureIds[]` | 特性 ID 列表 |
| `health` | 当前健康值 |
| `extraNeili` | 内力 |
| `consummateLevel` | 造诣等级 |
| `learnedLifeSkills[]` | 已学生活技能 |
| `learnedCombatSkills[]` | 已学战斗技能 |

> **建议：** 如需特定字段，优先使用 `back_code` + 链式进行精确定位。

---

# 12. 查询地块角色信息

**需求：** 获取地图上指定地块中的角色列表

## 12.1 获取地块上的角色 ID

**请求（1次）：**

```json
{
    "requestId": "block_chars_001",
    "type": "back_code",
    "params": {
        "entry": { "name": "GameData.Domains.DomainManager" },
        "chain": [
            { "step": "field", "name": "Map" },
            { "step": "method", "name": "GetBlockData", "argTypes": ["System.Int16", "System.Int16"], "args": [10, 581] },
            { "step": "field", "name": "CharacterSet" }
        ],
        "resultDepth": 7
    }
}
```

| 参数 | 类型 | 说明 |
|:----|:----|:-----|
| `resultDepth` | `7` | 需 ≥7 以穿透 `HashSet` 内部 Entry 结构 |

**响应解析：** JSON 中提取 `"value": 数字`（正整数）即为角色 ID。

**后续：** 用 `character_name` 或 `character_info` 获取姓名/详情。

## 12.2 获取门派地块信息（扩展）

当已知地块属于某个门派时，可通过以下流程获取门派全貌：

| 步骤 | 请求 | 说明 |
|:----|:-----|:-----|
| 12.2.1 | `back_code` → `Organization.GetSettlementIdByOrgTemplateId(tid)` | 通过门派模板 ID 查 SettlementId |
| 12.2.2 | `back_code` → `Map.GetBlockData` → `groupBlockList` | 查主地块获取所属地块组 |
| 12.2.3 | 遍历 blockList 查每个地块的 `CharacterSet` | 获取所有地块的角色 ID |
| 12.2.4 | `character_name` 逐个查名 | 获取姓名 |

**门派模板 ID 对照（`Organization_language.txt`）：**

| ID | 门派 |
|:--:|:-----|
| 0 | 无门无派 |
| 1 | 少林派 |
| 2 | 峨眉派 |
| 3 | 百花谷 |
| 4 | 武当派 |
| 5 | 元山派 |
| 6 | 狮相门 |
| 7 | 然山派 |
| 8 | 璇女派 |
| 9 | 铸剑山庄 |
| 10 | 空桑派 |
| 11 | 金刚宗 |
| 12 | 五仙教 |
| 13 | 界青门 |
| 14 | 伏龙坛 |

---

# 13. 查询当前 UI 层级结构

**需求：** 查看当前屏幕上所有显示的 UI 画布（Canvas），按玩家视角的渲染顺序（从远到近）排列。

> **原理：** Unity UI 的渲染顺序由 Canvas 组件的 `sortingOrder`（排序值）决定，值越小越远（先渲染），值越大越近（后渲染，遮挡前面的）。结合 `renderMode`（渲染模式）可判断 Canvas 是屏幕空间还是世界空间。

## 13.1 一键查询（推荐）

使用自动化脚本 `查询脚本/query_ui_hierarchy.py`，运行后自动完成所有 Canvas 的 `sortingOrder` 和 `renderMode` 查询并按层级排序：

```bash
python "查询脚本/query_ui_hierarchy.py"
```

脚本会依次：
1. `scene_hierarchy` 获取所有 Canvas 列表
2. 对每个活跃的 Canvas，用 `front_code` 查 `sortingOrder` 和 `renderMode`
3. 按 `sortingOrder` 从小到大排序输出

> **注意：** 运行时无需传参，确保游戏正在运行且在工作目录有读取权限即可。

## 13.2 手动查询步骤

如果需要手动分步查询，按以下流程：

```json
{
    "requestId": "ui_list_001",
    "type": "scene_hierarchy",
    "params": {
        "rootType": "all_canvases"
    }
}
```

从返回的 `data`（或 `longResultFile`）中得到每个 Canvas 的 `path` 和 `active` 状态。

### 步骤 2：查询单个 Canvas 的排序值

对每个主要的 Canvas，用 `front_code` 获取排序信息：

```json
{
    "requestId": "ui_order_001",
    "type": "front_code",
    "params": {
        "entry": { "name": "UnityEngine.GameObject" },
        "chain": [
            { "step": "method", "name": "Find", "argTypes": ["System.String"], "args": ["Camera_UIRoot/Canvas/LayerPopUp"] },
            { "step": "method", "name": "GetComponent<UnityEngine.Canvas>" },
            { "step": "property", "name": "sortingOrder" }
        ]
    }
}
```

返回示例：`600`

同时可查 `renderMode`（渲染模式）：

```json
{
    "requestId": "ui_rm_001",
    "type": "front_code",
    "params": {
        "entry": { "name": "UnityEngine.GameObject" },
        "chain": [
            { "step": "method", "name": "Find", "argTypes": ["System.String"], "args": ["Camera_UIRoot/Canvas/LayerPopUp"] },
            { "step": "method", "name": "GetComponent<UnityEngine.Canvas>" },
            { "step": "property", "name": "renderMode" }
        ]
    }
}
```

返回值说明：

| renderMode | 含义 |
|:----------|:-----|
| `0` | ScreenSpaceOverlay — 屏幕覆盖（最顶层） |
| `1` | ScreenSpaceCamera — 摄像机空间（可做特效） |
| `2` | WorldSpace — 世界空间（在地图/3D场景中） |

## 13.3 游戏 UI 层级参考（实测数据）

以下是从**世界地图界面**实测的 UI 层级，按 `sortingOrder` 从小到大排列（即从远到近、从底层到顶层）。**不同界面下 Canvas 的激活/隐藏不同**，运行一键脚本即可获取当前场景的实时层级。

| 层级 | sortingOrder | renderMode | 分组 | 用途 |
|:---:|:-----------:|:----------|:-----|:-----|
| 1 | **-32768** | ScreenSpaceCamera | `MaskBack` | 最底层遮罩 |
| 2 | **0** | ScreenSpaceCamera | `LayerBack` | 背景层（世界地图根部） |
| 3 | **0** | WorldSpace | `ViewWorldMap` | 世界地图（子物体含地块、角色等） |
| 4 | **0** | WorldSpace | `ViewBottom` | 底部工具栏（子物体） |
| 5 | **100** | ScreenSpaceCamera | `Canvas`(根) | 基础 Canvas |
| 6 | **100** | WorldSpace | `MapRawImage_Block` | 地图瓦片块 |
| 7 | **150~160** | WorldSpace | 路径/通用根/太吾/玩家 | 地图上的实体元素 |
| 8 | **185~190** | WorldSpace | `CommonRoot/TaiwuRoot/Player` | 通用物体/太吾/玩家标记 |
| 9 | **199** | WorldSpace | `Vignoverlay` | 地图渐晕 |
| 10 | **200** | ScreenSpaceCamera | `LayerMain` | 主 UI 层 |
| 11 | **211** | ScreenSpaceCamera | `UI_TaskPanelMain` | 任务面板 |
| 12 | **400** | ScreenSpaceCamera | `LayerPart` | 局部 UI（底部栏、资源栏、世界状态） |
| 13 | **600** | ScreenSpaceCamera | `LayerPopUp` | 弹窗层（角色面板、商店等） |
| 14 | **601** | ScreenSpaceCamera | 战斗相关 Camera | 技艺/战斗卡牌 |
| 15 | **800** | ScreenSpaceCamera | `LayerVeryTop` | 最顶层 UI（指引、进度等） |
| 16 | **1000** | ScreenSpaceCamera | `LayerTips` | 提示层（GM窗口、永久提示） |
| 17 | **1199** | ScreenSpaceCamera | `TipsArea` | 提示区域子层 |
| 18 | **1200** | ScreenSpaceCamera | `LayerGlow` | 发光特效层 |
| 19 | **32767** | ScreenSpaceCamera | `LayerCursor` | 鼠标光标 |
| 20 | **32767** | ScreenSpaceCamera | `LayerSpecial` & 各方向 Mask | 特殊层 + 边界遮罩 |

> **不同界面对比示例：**
> - **世界地图**：显示 `ViewWorldMap`、`MapRawImage_Block`、`CommonRoot`、`TaiwuRoot` 等 WorldSpace 地块元素
> - **角色面板**：`LayerPopUp` 下出现 `ViewCharacterMenu`、`ViewCharacterMenuEquip`、`ViewCharacterMenuInfo`
> - **建筑/制造**：`ViewWorldMap` 隐藏，出现 `ViewBuildingArea`、`ViewBuildingManage`、`ViewMake`，且 `SharedUIMask(order=999)` 遮罩层出现
> - **商店**：`LayerPopUp` 下出现 `ViewShop`，`UI_ShopConfirm` 二次确认弹窗
>
> 实际查询时运行一键脚本即可获得当前场景的精确层级。
